<%= form_for [ @restaurant, @review ] do |f| %>
  <%= f.text_field :content %>
  <%= f.select(:rating, [0,1,2,3,4,5], {include_blank: "Select a category..."}, { :class => 'form_select' }) %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
